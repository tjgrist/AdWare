@model IEnumerable<AdAtTheRightTime.Models.Business>

@{
    ViewBag.Title = "ViewLikedBusinesses";
}

<h2>ViewLikedBusinesses</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>

        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Check em Out!","Userview", "Businesses", new { id=item.BusinessId }, null) 
        </td>
    </tr>
}

</table>

@Html.ActionLink("Back to your page", "UserView", "Users")
 
